# .env.example
# Required
DATABASE_URL=postgres://username:password@host:5432/dbname
NEXT_PUBLIC_BASE_URL=https://your-deployment-url.example    # used by client for copy/share links (optional)
# Optional (increase pool size for dev)
PG_MAX_CLIENTS=6
